<%= simple_form_for [@restaurant, @review], html: { class: 'form-horizontal' }, wrapper: :horizontal_form do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: (0..5).to_a, selected: 3 %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
